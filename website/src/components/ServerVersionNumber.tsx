import * as React from 'react'

// Do not delete/refactor the following line! It's used by sourcegraph/sourcegraph's CI
// to automate releases.
const versionNumber = '2.11.2'

export const ServerVersionNumber: React.StatelessComponent<{}> = () => <span>{versionNumber}</span>
